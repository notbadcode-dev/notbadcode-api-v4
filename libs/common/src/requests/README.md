# Common Requests

DTOs de request reutilizables entre servicios.

## Archivos

- `paginated-request.ts`: contrato común para endpoints `POST .../paginated`.
- `index.ts`

## Regla

Si cambia `PaginatedRequest`, revisar handlers paginados y documentación de contratos.
